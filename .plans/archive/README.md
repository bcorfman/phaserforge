# Archived Plans And Mockups

This directory is historical context only.

Files here may be:

- superseded by newer implementation
- partially implemented
- exploratory but never completed
- useful for background without being safe as current product guidance

Do not use this directory as the primary source of truth for new implementation. Check current tests, current code, `.repo-memory/`, and the small top-level `.plans/` surface first.
